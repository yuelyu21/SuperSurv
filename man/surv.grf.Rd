% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrapper-additional.R
\name{surv.grf}
\alias{surv.grf}
\title{Generalized Random Survival Forest Learner}
\usage{
surv.grf(
  time,
  event,
  X,
  newdata = NULL,
  new.times,
  obsWeights = NULL,
  id = NULL,
  num.trees = 1000L,
  mtry = NULL,
  min.node.size = 15L,
  honesty = TRUE,
  prediction.type = c("Kaplan-Meier", "Nelson-Aalen"),
  seed = 1L,
  ...
)
}
\arguments{
\item{time}{Observed follow-up time.}

\item{event}{Observed event indicator.}

\item{X}{Training covariate data frame.}

\item{newdata}{Covariate data frame used for prediction.}

\item{new.times}{Times at which survival probabilities are requested.}

\item{obsWeights}{Optional non-negative observation weights.}

\item{id}{Currently ignored.}

\item{num.trees}{Number of trees.}

\item{mtry}{Number of candidate variables considered at each split.}

\item{min.node.size}{Minimum terminal-node size.}

\item{honesty}{Whether to use honest sample splitting.}

\item{prediction.type}{Either \code{"Kaplan-Meier"} or \code{"Nelson-Aalen"}.}

\item{seed}{Integer random seed passed to \code{grf}.}

\item{...}{Additional arguments passed to \code{\link[grf:survival_forest]{grf::survival_forest()}}.}
}
\value{
A list with numeric matrix \code{pred} and fitted object \code{fit}.
}
\description{
Fits an honest generalized random survival forest using
\code{\link[grf:survival_forest]{grf::survival_forest()}} and returns conditional survival curves.
}
\examples{
if (requireNamespace("grf", quietly = TRUE)) {
  data("metabric", package = "SuperSurv")
  dat <- metabric[1:40, ]
  X <- dat[, grep("^x", names(dat))[1:3], drop = FALSE]
  fit <- surv.grf(
    dat$duration, dat$event, X, X[1:4, , drop = FALSE],
    c(50, 100), num.trees = 50, seed = 1
  )
  dim(fit$pred)
}
}
