% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrapper-additional.R
\name{surv.coxtime}
\alias{surv.coxtime}
\title{Experimental Cox-Time Neural Survival Learner}
\usage{
surv.coxtime(
  time,
  event,
  X,
  newdata = NULL,
  new.times,
  obsWeights = NULL,
  id = NULL,
  num_nodes = c(32L, 32L),
  activation = "relu",
  batch_norm = TRUE,
  dropout = NULL,
  epochs = 100L,
  batch_size = 128L,
  device = NULL,
  verbose = FALSE,
  seed = 1L,
  standardize_time = TRUE,
  ...
)
}
\arguments{
\item{time}{Observed follow-up time.}

\item{event}{Observed event indicator.}

\item{X}{Training covariate data frame.}

\item{newdata}{Covariate data frame used for prediction.}

\item{new.times}{Times at which survival probabilities are requested.}

\item{obsWeights}{Optional non-negative observation weights.}

\item{id}{Currently ignored.}

\item{num_nodes}{Positive integers giving hidden-layer sizes.}

\item{activation}{Neural-network activation name.}

\item{batch_norm}{Whether to use batch normalization.}

\item{dropout}{Optional dropout probability in \verb{[0, 1)}.}

\item{epochs}{Number of training epochs.}

\item{batch_size}{Training and prediction batch size.}

\item{device}{Optional device passed to \code{survivalmodels}.}

\item{verbose}{Whether the Python backend should print training progress.}

\item{seed}{Positive integer used for Python, NumPy, and torch random-number
generators.}

\item{standardize_time}{Whether to standardize outcome times using the
training-only Cox-Time label transformation. Predictions are returned on
the original time scale.}

\item{...}{Additional named fitting arguments passed to
\code{\link[survivalmodels:coxtime]{survivalmodels::coxtime()}}, such as \code{frac} for an internal training-fold
validation split or \code{early_stopping}. Outcomes and features are managed
by the adapter.}
}
\value{
A list with numeric survival matrix \code{pred} and fitted object \code{fit}.
}
\description{
Fits a neural Cox-Time model allowing time-dependent covariate effects through
\code{\link[survivalmodels:coxtime]{survivalmodels::coxtime()}}. This optional adapter requires Python \code{torch},
\code{torchtuples}, and \code{pycox} and supports only uniform observation weights.
}
\details{
Native survival curves are evaluated as right-continuous steps on
the backend's time grid, using survival one before the first grid point
and the final available value beyond the last point. This is not a claim
of reliable extrapolation beyond observed follow-up. Python-backed fitted
objects are intended for reuse within the active R/Python session; plain
\code{saveRDS()} is not a portable persistence format for Python objects.
}
\examples{
if (interactive() && requireNamespace("survivalmodels", quietly = TRUE) &&
    requireNamespace("reticulate", quietly = TRUE) &&
    reticulate::py_module_available("pycox")) {
  data("metabric", package = "SuperSurv")
  dat <- metabric[1:60, ]
  X <- dat[, "x1", drop = FALSE]
  fit <- surv.coxtime(dat$duration, dat$event, X,
                      X[1:3, , drop = FALSE], c(50, 100),
                      epochs = 2, batch_norm = FALSE, device = "cpu")
  dim(fit$pred)
}
}
