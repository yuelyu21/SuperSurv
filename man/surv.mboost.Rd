% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrapper-additional.R
\name{surv.mboost}
\alias{surv.mboost}
\title{Component-Wise Cox Boosting Learner}
\usage{
surv.mboost(
  time,
  event,
  X,
  newdata = NULL,
  new.times,
  obsWeights = NULL,
  id = NULL,
  mstop = 100L,
  nu = 0.1,
  center = FALSE,
  ties = c("breslow", "efron"),
  survival_transform = c("exponential", "product_limit"),
  ...
)
}
\arguments{
\item{time}{Observed follow-up time.}

\item{event}{Observed event indicator.}

\item{X}{Training covariate data frame.}

\item{newdata}{Covariate data frame used for prediction.}

\item{new.times}{Times at which survival probabilities are requested.}

\item{obsWeights}{Optional non-negative observation weights.}

\item{id}{Currently ignored.}

\item{mstop}{Number of boosting iterations.}

\item{nu}{Boosting step size.}

\item{center}{Whether to center component-wise base learners.}

\item{ties}{Tied-event approximation for risk-score calibration.}

\item{survival_transform}{Transformation from calibrated hazard increments
to survival probabilities.}

\item{...}{Additional arguments passed to \code{\link[mboost:glmboost]{mboost::glmboost()}}.}
}
\value{
A list with numeric matrix \code{pred} and fitted object \code{fit}.
}
\description{
Fits a weighted component-wise Cox proportional-hazards model using
\code{\link[mboost:glmboost]{mboost::glmboost()}} and converts its risk score to survival probabilities
using SuperSurv's weighted baseline-hazard calibration.
}
\examples{
if (requireNamespace("mboost", quietly = TRUE)) {
  data("metabric", package = "SuperSurv")
  dat <- metabric[1:40, ]
  X <- dat[, grep("^x", names(dat))[1:3], drop = FALSE]
  fit <- surv.mboost(
    dat$duration, dat$event, X, X[1:4, , drop = FALSE],
    c(50, 100), mstop = 20
  )
  dim(fit$pred)
}
}
