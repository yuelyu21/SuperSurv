% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrapper-additional.R
\name{surv.deephit}
\alias{surv.deephit}
\title{Experimental DeepHit Learner}
\usage{
surv.deephit(
  time,
  event,
  X,
  newdata = NULL,
  new.times,
  obsWeights = NULL,
  id = NULL,
  num_nodes = c(32L, 32L),
  activation = "relu",
  batch_norm = TRUE,
  dropout = NULL,
  epochs = 100L,
  batch_size = 128L,
  device = NULL,
  verbose = FALSE,
  seed = 1L,
  cuts = 20L,
  cutpoints = NULL,
  scheme = c("equidistant", "quantiles"),
  mod_alpha = 0.2,
  sigma = 0.1,
  ...
)
}
\arguments{
\item{time}{Observed follow-up time.}

\item{event}{Observed event indicator.}

\item{X}{Training covariate data frame.}

\item{newdata}{Covariate data frame used for prediction.}

\item{new.times}{Times at which survival probabilities are requested.}

\item{obsWeights}{Optional non-negative observation weights.}

\item{id}{Currently ignored.}

\item{num_nodes}{Positive integers giving hidden-layer sizes.}

\item{activation}{Neural-network activation name.}

\item{batch_norm}{Whether to use batch normalization.}

\item{dropout}{Optional dropout probability in \verb{[0, 1)}.}

\item{epochs}{Number of training epochs.}

\item{batch_size}{Training and prediction batch size.}

\item{device}{Optional device passed to \code{survivalmodels}.}

\item{verbose}{Whether the Python backend should print training progress.}

\item{seed}{Positive integer used for Python, NumPy, and torch random-number
generators.}

\item{cuts}{Number of discrete time intervals, or a vector of cut points
supplied through \code{cutpoints}.}

\item{cutpoints}{Optional numeric vector of cut points.}

\item{scheme}{Cut-point scheme used when \code{cutpoints} is \code{NULL}.}

\item{mod_alpha}{Weight placed on the likelihood component of the DeepHit
objective.}

\item{sigma}{Ranking-loss bandwidth.}

\item{...}{Additional arguments passed to \code{\link[survivalmodels:deephit]{survivalmodels::deephit()}}.}
}
\value{
A list with numeric matrix \code{pred} and fitted object \code{fit}.
}
\description{
Fits a single-event discrete-time DeepHit neural network through
\code{\link[survivalmodels:deephit]{survivalmodels::deephit()}}. This optional adapter requires a Python
environment containing \code{torch}, \code{torchtuples}, and \code{pycox} and currently
supports only uniform observation weights.
}
\details{
Fitted Python objects can be reused in the active R/Python session.
Plain \code{saveRDS()} is not a portable persistence format for these objects.
Native survival curves are mapped to requested times as right-continuous
steps; see \code{\link[=surv.coxtime]{surv.coxtime()}} for the boundary convention.
}
\examples{
if (interactive() && requireNamespace("survivalmodels", quietly = TRUE) &&
    requireNamespace("reticulate", quietly = TRUE) &&
    reticulate::py_module_available("pycox")) {
  data("metabric", package = "SuperSurv")
  dat <- metabric[1:60, ]
  X <- dat[, grep("^x", names(dat))[1:3], drop = FALSE]
  fit <- surv.deephit(
    dat$duration, dat$event, X, X[1:4, , drop = FALSE],
    c(50, 100), cuts = 10, epochs = 2, seed = 1
  )
  dim(fit$pred)
}
}
