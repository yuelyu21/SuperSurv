% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrapper.R
\name{predict.surv.bart}
\alias{predict.surv.bart}
\title{Prediction function for BART}
\usage{
\method{predict}{surv.bart}(object, newdata, new.times, ...)
}
\arguments{
\item{object}{Fitted \code{surv.bart} object.}

\item{newdata}{New covariate data.frame to predict on.}

\item{new.times}{Times to predict.}

\item{...}{Additional ignored arguments.}
}
\value{
A numeric matrix of predicted survival probabilities, where rows correspond
to the observations in \code{newdata} and columns correspond to the evaluation
times in \code{new.times}.
}
\description{
Obtains predicted survivals from a fitted \code{surv.bart} object.
Manually expands the time grid to bypass the 10-column expectation.
}
\examples{
if (requireNamespace("BART", quietly = TRUE)) {
  data("metabric", package = "SuperSurv")
  dat <- metabric[1:20, ]
  x_cols <- grep("^x", names(dat))[1:3]
  X <- dat[, x_cols, drop = FALSE]
  newX <- X[1:5, , drop = FALSE]
  times <- seq(50, 150, by = 50)

  fit <- surv.bart(
    time = dat$duration,
    event = dat$event,
    X = X,
    newdata = newX,
    new.times = times,
    ntree = 3,
    ndpost = 5,
    nskip = 5
  )

  pred <- fit$pred
  dim(pred)
}
}
