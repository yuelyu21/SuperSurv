% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrapper-flexible.R
\name{surv.survPen}
\alias{surv.survPen}
\title{Penalized Smooth Hazard Survival Learner}
\usage{
surv.survPen(
  time,
  event,
  X,
  newdata = NULL,
  new.times,
  obsWeights = NULL,
  id = NULL,
  formula = NULL,
  baseline.df = 4L,
  n.legendre = 50L,
  ...
)
}
\arguments{
\item{time}{Observed follow-up time.}

\item{event}{Observed event indicator.}

\item{X}{Training covariate data frame.}

\item{newdata}{Covariate data frame used for prediction.}

\item{new.times}{Times at which survival probabilities are requested.}

\item{obsWeights}{Optional non-negative observation weights.}

\item{id}{Currently ignored.}

\item{formula}{Optional one-sided hazard formula using feature names and
\code{.supersurv_time}. For example,
\code{~ smf(.supersurv_time, df = 4) + smf(x1, df = 4) + x2}, or
\code{~ tensor(.supersurv_time, x1, df = c(4, 4)) + x2} for a time-varying effect.
The \code{survPen} constructors \code{smf}, \code{tensor}, \code{tint}, and \code{rd} are available
without attaching the backend package. Formula variables must come from
the current training features or \code{.supersurv_time}.}

\item{baseline.df}{Baseline smooth degrees of freedom, at least 3. Used only
when \code{formula} is NULL; the default adds linear terms for all features to
\code{smf(.supersurv_time, df = baseline.df)}.}

\item{n.legendre}{Positive integer quadrature order for both fitting and
survival prediction. Increase this to check integration accuracy.}

\item{...}{Additional named fitting controls passed to \code{\link[survPen:survPen]{survPen::survPen()}},
such as \code{lambda}, \code{method}, or \code{max.it.beta}.}
}
\value{
A list with numeric survival matrix \code{pred} and fitted object \code{fit}.
}
\description{
Fits an overall hazard model using \code{\link[survPen:survPen]{survPen::survPen()}} and returns its native
survival probabilities. Only single-event, right-censored outcomes and uniform
observation weights are supported.
}
\details{
Nonuniform observation weights are rejected because the backend does
not expose case-weighted fitting. The adapter does not implement net survival,
relative mortality, or left truncation. Survival curves that increase beyond
numerical tolerance cause an error; increase quadrature accuracy rather than
silently projecting a materially invalid curve. Custom formulas must remain
valid after feature screening; use \code{screen.all} when explicitly naming features.
}
\examples{
if (requireNamespace("survPen", quietly = TRUE)) {
  data("metabric", package = "SuperSurv")
  dat <- metabric[1:80, ]
  X <- dat[, "x1", drop = FALSE]
  fit <- surv.survPen(dat$duration, dat$event, X,
                      X[1:3, , drop = FALSE], c(50, 100), baseline.df = 3)
  dim(fit$pred)
}
}
