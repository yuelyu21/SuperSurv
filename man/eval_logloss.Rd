% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluation.R
\name{eval_logloss}
\alias{eval_logloss}
\title{IPCW Log-Loss and Integrated IPCW Log-Loss}
\usage{
eval_logloss(
  time,
  event,
  S_mat,
  times,
  tmin = min(times),
  tmax = max(times),
  ipcw_floor = 1e-06,
  eps = 1e-10,
  ipcw_cap = Inf,
  G_T_left = NULL,
  G_times = NULL
)
}
\arguments{
\item{time}{Numeric vector of observed follow-up times.}

\item{event}{Numeric vector of event indicators (1 = event, 0 = censored).}

\item{S_mat}{A numeric matrix of predicted survival probabilities
(rows = observations, columns = time points).}

\item{times}{Numeric vector of evaluation times matching the columns of \code{S_mat}.}

\item{tmin}{Numeric. Lower bound for IBS integration. Defaults to \code{min(times)}.}

\item{tmax}{Numeric. Upper bound for IBS integration. Defaults to \code{max(times)}.}

\item{ipcw_floor}{Positive lower bound applied to censoring survival before
inversion.}

\item{eps}{Probability clipping constant in \code{(0, 0.5)}.}

\item{ipcw_cap}{Largest IPCW contribution. Use \code{Inf} for no cap.}

\item{G_T_left}{Optional numeric vector containing subject-specific
\eqn{G(T_i-\mid X_i)} values. Supply together with \code{G_times}.}

\item{G_times}{Optional numeric matrix with the dimensions of \code{S_mat}
containing \eqn{G(t\mid X_i)}, or a vector aligned with \code{times} for a
common censoring survival curve. Supply together with \code{G_T_left}.}
}
\value{
A list containing \code{logloss_scores}, \code{integrated_logloss},
\code{times}, and \code{diagnostics}. Diagnostics report the effective
denominator threshold, intervention counts, weight quantiles, effective
sample sizes, and the fraction of weighted loss contributed by capped
rows, separately for failure and survivor terms.
}
\description{
Evaluates the two-term inverse-probability-of-censoring weighted log-loss.
By default, censoring survival is estimated using marginal reverse
Kaplan-Meier. Conditional or externally estimated censoring survival values
can instead be supplied explicitly. Probability clipping is applied only
inside logarithms.
}
